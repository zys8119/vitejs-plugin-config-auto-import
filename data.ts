import {ViteOptions} from "./types";
export const config0 = {
    "test": {}
}
export const active0 = 'test'
export const defaultConfig0:Partial<ViteOptions> = {
    globalName:'$config',
    configTypeName:'$type',
}
